## What does this change?

<!-- One or two sentences on what changed and why. -->

## How was this tested?

<!-- e.g. "clicked through the Servers page and added a stdio server", "npm run build clean" -->

## Checklist

- [ ] `npx tsc --noEmit` passes
- [ ] `npm run lint` passes
- [ ] `npm run build` passes
- [ ] I manually tested the affected UI/flow in a browser (for UI changes)
